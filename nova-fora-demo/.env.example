# Copy to .env.local for local dev (not committed).
# In production (EasyPanel), set this as a BUILD ARG on the Docker service.

# Points Vite to the FastAPI backend.
# Local dev (docker-compose or uvicorn on host):
#   VITE_API_BASE_URL=http://localhost:8000
# Production:
#   VITE_API_BASE_URL=https://nova-fora-api.vamj8y.easypanel.host
VITE_API_BASE_URL=https://nova-fora-api.vamj8y.easypanel.host
